/**
 * @file
 * Defines 'Examples' collections and helpers.
 */
/* globals Examples:true, Mongo */

// Declare  collection.


/*
// Define schema for Examples collection.
tags.attachSchema({
  name: {
    type: String,
    label: 'Title',
    max: 50
  }
});

// Add helpers to lists collection object.
tags.helpers({
  headShort: function() {
    return this.name.substring(0, 50);
  }
});
*/